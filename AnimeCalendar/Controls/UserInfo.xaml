<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AnimeCalendar.Controls.UserInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AnimeCalendar.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock>
            <Run Text="{x:Bind User.Nickname}" />
            <Run Text="{x:Bind username}" Foreground="{ThemeResource TextFillColorTertiary}" />
        </TextBlock>

        <TextBlock Text="{x:Bind User.Sign}" FontSize="12" Foreground="Gray" />
    </StackPanel>
</UserControl>
